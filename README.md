# caffe_model_pruning
Repository containing scripts for caffe net-surgery for channel pruning

This python script does the caffe model pruning based on L1 norm of each layers.
This works for very simple networks without skip connections.
